# Backlog: cast pinning, polls, quotes, accessibility

## Summary

Smaller ideas from the 2026-07-16 state-of-the-project review, parked here
until picked up (split into real issues when started).

- **Cast pinning**: let a user force which character plays an account, e.g.
  `#cast=alice@inst:tux` hash param feeding `makeCast`.
- **Polls**: render poll options as a list inside the balloon (with vote
  counts when closed).
- **Quote posts**: Pleroma quotes could render as a small inset narration
  box referencing the quoted author.
- **Accessibility**: the strip is div soup to a screen reader — panel
  `role`/labels, balloon reading order, alt text beyond character names.

## Requirements

- None yet; promote an item to its own issue before working on it.

## Acceptance Criteria

- N/A (tracking issue).
